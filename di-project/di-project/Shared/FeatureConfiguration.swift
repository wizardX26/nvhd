import Foundation

struct FeatureConfiguration {
    var prefetchEnabled = true
    // Enable only after supplying the corresponding platform/backend adapters.
    var iapEnabled = false
    var adsEnabled = false
}
